§NAME: Ïåğåâîä èç DOS êîäèğîâêè â Windows è íàîáîğîò
§AUTHOR: Adil
§EMAIL: kazimoff@yahoo.com

Ôóíêöèè ìåíÿşò ïåğåäàâàåìóş èì ñòğîêó.
Äëÿ óäîáñòâà âîçâğàùàşò óêàçàòåëü íà ïåğåäàâàåìóş ñòğîêó.
[CODE]
//èç ÄÎÑ â Windows
char* Decode_DOS_to_Win(char * str)
{
    unsigned char *cstr=str;//"unsigned" - ÷òîá èçáåæàòü ïğåäóïğåæäåíèé êîìï-ğà
    for(; *cstr; cstr++)
    {
        if(*cstr>=128 && *cstr<=175)
            *cstr+=64;
        else if(*cstr>=224 && *cstr<=239)
            *cstr+=16;
        else if(*cstr==252)
            *cstr=185;
    }
    return str;
}
//----------------------------------------------------

//èç Windows â ÄÎÑ
char* Decode_Win_to_DOS(char * str)
{
    unsigned char *cstr=str;
    for(;*cstr;cstr++)
    {
        if(*cstr>=240)
            *cstr-=16;
        else if(*cstr>=192)
            *cstr-=64;
        else if(*cstr==185)
            *cstr=252;
    }
    return str;
}
//----------------------------------------------------
[/CODE]