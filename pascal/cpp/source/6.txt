§NAME: Как промэпить сетевой диск
§AUTHOR: Alfa
§EMAIL: alfa7@pisem.net
[code]NETRESOURCE netres;
ZeroMemory(&netres, sizeof(netres));

    netres.dwScope=RESOURCE_GLOBALNET;
    netres.dwType=RESOURCETYPE_DISK;
    netres.dwDisplayType=RESOURCEDISPLAYTYPE_GENERIC;
    netres.dwUsage=RESOURCEUSAGE_CONNECTABLE;
    netres.lpLocalName=LocalDrive;
    netres.lpRemoteName=ShareName;
    netres.lpComment="";
    netres.lpProvider=NULL;

    WNetAddConnection2(&netres, 
                        (LPSTR) NULL,         // Пароль к диску (в данном случае юзаем текущий пароль юзера)
                        (LPSTR) NULL,         // Юзаем текущее имя пользователя.
                        CONNECT_UPDATE_PROFILE
                        (LPSTR) NULL,
                        );[/code]