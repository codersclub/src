

$db_type = 'mysql';		# Тип базы данных
$db_name = 'cms';		# Имя базы данных
$db_host = 'localhost';		# Адрес хоста базы данных
$db_user = 'root';		# Логин
$db_password = '';		# Пароль

$img_dir	= "/cms/img";
$cms_dir	= "/cms";

$tmp_file_for_check_url = '/tmp/page.html';	# Временный файл для проверки ссылок

1;